const initialState = {
    friendsName: [
        {name: "Victor", id: 1},
        {name: "Vadim", id: 2},
        {name: "Max", id: 3}
    ]
}

const sidebarReducer= (state = initialState, action) => {

    return state;
}

export default sidebarReducer;